export default function NotFound(){
    return (
        //Used in globals.css
        <main className="not-found">
            <h1>Page Not Found</h1>
            <p>Could not found requested page</p>
        </main>
    )
}